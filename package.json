{
  "name": "kindful-js",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "private": false,
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "yarn tsc --build",
    "dev": "yarn tsc --watch",
    "dev:local": "yarn tsc --watch"
  },
  "devDependencies": {
    "prettier": "^2.5.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.3"
  }
}